/**
 * HiGo Abuja — @higo/shared-types
 * Public barrel. Import from '@higo/shared-types' everywhere.
 *
 * Published by Agent 1 (Backend Foundation) BEFORE any other agent starts.
 * Treat this package as a contract: additive changes only without a coordinated
 * version bump across all consumers.
 */

export * from './domain.types';
export * from './api.types';
export * from './socket.types';
